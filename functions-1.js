function reheatPizza(numSlices) {
  // code that figures out reheat settings!
}

function reverseString(reverseMe) {
  // code to reverse a string!
}

function doubleGreeting(name, otherName) {
  // code to greet two people!
}

// accepts no parameters! parenthesis are empty
function sayHello() {
  let message = "Hello!";
  return message;
}

console.log(sayHello());

function findAverage(x, y) { // paramaters are x and y, they are defined in the function declaration
  let answer = (x + y) / 2;
  return answer;
}

let avg = findAverage(5, 9); // 5 and 9 are passed in as function arguments
console.log(avg);